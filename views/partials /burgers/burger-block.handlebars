<li>
	{{burger_name}}

    {{#unless devoured }}<button id="{{id}}" class="devourBtn btn btn-sm btn-danger " data-id="{{id}}" data-devour="true">
        Devour</button>{{/unless}}

	<button class="deleteburger" data-id="{{this.id}}">DELETE!</button>
	
</li>
